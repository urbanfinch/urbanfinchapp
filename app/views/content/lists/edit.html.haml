%section{:id => 'content_lists_edit'}
  .sidebar
    .sidebar_header
      %h2 Edit List
    .sidebar_nav
      %nav.content_nav
        %div{:class => 'selected'}
          %a{:href => '#list'} List
        %div
          %a{:href => '#bullets'} Bullets
        - if current_user.administrator?
          %div
            %a{:href => '#administrator'} Administrator
    .sidebar_buttons
      = submit_tag 'Save', :form => "edit_list_#{@list.id}"
      = link_to 'Back to lists', content_lists_path
  .content
    = render :partial => 'form', :locals => {:list => @list, :url => content_list_path}